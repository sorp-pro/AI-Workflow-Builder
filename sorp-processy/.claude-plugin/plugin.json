{
  "name": "sorp-processy",
  "version": "0.2.0",
  "description": "Process models for SORP: builds a runnable model of a map process, derives and runs every test scenario, publishes open questions to the SORP stand, lets anyone with a stand key answer them right in Claude or in the stand web form, applies answers back and releases models to the simulation stand with UI tests. Code only — map data stays in your working folder.",
  "author": {
    "name": "SORP"
  },
  "keywords": ["processes", "models", "scenarios", "simulation", "questions"]
}
